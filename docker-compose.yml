version: "3.9"

services:
  api:
    build: .
    container_name: bookstore-api
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
    volumes:
      - db-data:/app/data

volumes:
  db-data: